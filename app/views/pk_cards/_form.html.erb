<%= form_with(model: pk_card, local: true) do |form| %>
  <% if pk_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pk_card.errors.count, "error") %> prohibited this pk_card from being saved:</h2>

      <ul>
      <% pk_card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
